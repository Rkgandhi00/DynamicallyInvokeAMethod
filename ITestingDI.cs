namespace InvokeDynamically
{
    public interface ITestingDI
    {
        // Interface to check the how we can call class which has implementation of
        // Dependency Injection with parameter as an Interface
        void InvokeMe();
    }
}
